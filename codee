import LoadingSpinner from "./components/LoadingSpinner";

function DashboardHome({ summaryData }) {
  if (!summaryData || summaryData.length === 0) {
    return <LoadingSpinner message="Loading dashboard..." />;
  }

  return <div>...dashboard...</div>;
}