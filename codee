- (m.zone && m.zone.includes(filterZone)) ||
- baseLocationByRole[role].includes(filterZone)
+ (m.zone && m.zone.toLowerCase() === filterZone.toLowerCase()) ||
+ (baseLocationByRole[role] && baseLocationByRole[role].toLowerCase() === filterZone.toLowerCase())




....



const members = filterZone
- ? membersRaw.filter(m =>
-     m.present && (
-       (m.zone && m.zone.includes(filterZone)) ||
-       baseLocationByRole[role].includes(filterZone)
-     )
-   )
+ ? membersRaw.filter(m =>
+     m.present && (
+       (m.zone && m.zone.toLowerCase() === filterZone.toLowerCase()) ||
+       (baseLocationByRole[role] && baseLocationByRole[role].toLowerCase() === filterZone.toLowerCase())
+     )
+   )
   : membersRaw;