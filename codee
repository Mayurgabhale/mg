i want to get this page only if 
select this http://localhost:3000/partition/US.CO.OBS

then only redirect to this page not for alllUS.CO.OBS
only for this http://localhost:3000/partition/US.CO.OBS


const handlePartitionChange = newPartition => {
  if (!newPartition) return navigate('/');

  // Special handling for Denver partition - only redirect when going to base partition route
  if (newPartition === 'US.CO.OBS' && suffixSegments.length === 0) {
    window.location.href = 'http://10.199.22.57:3012/';
    return;
  }

  // Always point at the new partition
  let path = `/partition/${encodeURIComponent(newPartition)}`;
  
  // If we were on “history”, keep the history view
  if (isHistoryPage) path += '/history';

  navigate(path);
};
