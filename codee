const [showCenterUploads, setShowCenterUploads] = useState(false);




....



<IconButton
  sx={{ color: '#FFF', ml: 1 }}
  onClick={() => setShowCenterUploads(true)}
>
  <i className="bi bi-cloud-arrow-up-fill"></i>
</IconButton>



....



<Dialog
  open={showCenterUploads}
  onClose={() => setShowCenterUploads(false)}
  maxWidth="xs"
  fullWidth
  PaperProps={{
    sx: { bgcolor: 'black', borderRadius: 2, p: 1 }
  }}
>
  <DialogTitle
    sx={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', color: '#FFC72C' }}
  >
    Uploads & Export
    <IconButton onClick={() => setShowCenterUploads(false)} sx={{ color: '#fff' }}>
      <CloseIcon />
    </IconButton>
  </DialogTitle>

  <DialogContent sx={{ display: 'flex', justifyContent: 'center', p: 2 }}>
    <Box sx={{ width: '100%', display: 'flex', flexDirection: 'column', gap: 1 }}>
      <Button
        variant="contained"
        size="small"
        startIcon={<UploadFileIcon />}
        onClick={() => fileInputEmpRef.current?.click()}
        sx={{ fontWeight: 700 }}
      >
        Upload Active Employee Sheet
      </Button>

      <Button
        variant="contained"
        size="small"
        startIcon={<UploadFileIcon />}
        onClick={() => fileInputContrRef.current?.click()}
        sx={{ fontWeight: 700 }}
      >
        Upload Active Contractor Sheet
      </Button>

      <Button
        variant="contained"
        size="small"
        startIcon={<DescriptionIcon />}
        onClick={() => { exportUploadedSheets(); setShowCenterUploads(false); }}
        sx={{ fontWeight: 700 }}
      >
        Export Comparison → Report
      </Button>
    </Box>
  </DialogContent>
</Dialog>